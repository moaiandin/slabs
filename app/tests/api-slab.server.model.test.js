'use strict';

/**
 * Module dependencies.
 */
var should = require('should'),
	mongoose = require('mongoose'),
	User = mongoose.model('User'),
	ApiSlab = mongoose.model('ApiSlab');

/**
 * Globals
 */
var user, apiSlab;

/**
 * Unit tests
 */
describe('Api slab Model Unit Tests:', function() {
	beforeEach(function(done) {

	});

	describe('Method Load API Slabs', function() {

	});

	afterEach(function(done) { 

		done();
	});
});
